## Assignment

Create a function that takes:

- two numbers

The function should:

- calculate the result
- display the result on the page in the format: "5 × 3 = 15"

The function should be reusable and work with different numbers each time it is called.
